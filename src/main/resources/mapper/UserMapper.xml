<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mingletrade.mingletrade.mapper.UserMapper">

	<select id="findByEmail" parameterType="string" resultType="map">
		SELECT * FROM USERS WHERE EMAIL = #{email}
	</select>

	<insert id="insertUser" parameterType="map">
		INSERT INTO USERS (EMAIL, NAME, PICTURE, PROVIDER, NICKNAME, PROFILE_IMAGE)
		VALUES (#{email}, #{name}, #{picture}, #{provider}, #{nickname}, #{profileImage})
	</insert>
	
	<update id="updateNickname"  parameterType="com.mingletrade.mingletrade.domain.User">
		UPDATE USERS 
		SET	NICKNAME = #{nickname} 
		WHERE EMAIL = #{email} 				
	</update>
	
	<update id="updateProfileImage">
		UPDATE USERS 
		SET PROFILE_IMAGE = #{profileImage} 
		WHERE EMAIL = #{email}
	</update>

</mapper>